#ifndef PEACHCOMPILER_H
  #define PEACHCOMPILER_H
  struct compile_process
  {
    /* data */
  };

  int compile_file(const char*filename , const char* out_filename ,int flags);//(编译程序名，编译后输出程序名，flag)

#endif
